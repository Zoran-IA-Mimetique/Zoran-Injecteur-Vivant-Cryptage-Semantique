services:
  zoran-injector:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ./:/app
    environment:
      - ZORAN_SEMKEY=${ZORAN_SEMKEY:-DEMO-KEY-NOT-SECURE}
    command: ["python","demo/run_demo.py"]
